@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
 
@model Check.Models.Signup
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Login</title>

    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />

    <link href="~/Content/modern-business.css" rel="stylesheet" />

    <link href="~/Content/font-awesome.min.css" rel="stylesheet" />
    <script src="~/Scripts/bootstrap.js"></script>
</head>
<body>
    <div class="container">
        <h1> Login to Your Acount</h1>
        <hr>
        <div class="row">
           
            <div class="col-md-7 personal-info">
                <div class="alert alert-info alert-danger">
                    <a class="panel-close close" data-dismiss="alert"></a>
                    <i class="fa fa-coffee"></i>
               Login 
                </div>
                
                @*@using (Html.BeginForm("AfterLogin", "Login", FormMethod.Post, new  {@class="form-horizontal" }))
                {
                    @Html.HiddenFor(m => m.Id)
                    @Html.ValidationMessageFor(m=>m.Email);
                    @Html.LabelFor(m => m.Email, new { @class = "col-lg-1 control-label" })
                    
                    @Html.TextBoxFor(x => x.Email, new { @class = "form-control" })
                    @Html.LabelFor(m => m.Password, new { @class = "col-md-1 control-label" })
                    
                    @Html.TextBoxFor(x => x.Password, new { @class = "form-control" });
                   <br>
                    <input type="submit" class="btn btn-success" value="Login">
                }*@
           
             <form class="form-horizontal" role="form" action="/Login/AfterLogin">


                    <div class="form-group">
                        <label class="col-lg-3 control-label">Email:</label>

                        <div class="col-lg-8">
                            <input class="form-control" type="email" name="Email" required="required">
                           <h2> @ViewBag.error</h2>
                        </div>
                    </div>


                    <div class="form-group">
                        <label class="col-md-3 control-label">Password:</label>
                        <div class="col-md-8">
                            <input class="form-control" type="password" name="Password" required="required">
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="col-md-3 control-label"></label>
                        <div class="col-md-8">
                            <input type="submit" class="btn btn-success" value="Login">
                            <span></span>
                            <input type="reset" class="btn btn-default" value="Cancel">
                        </div>
                    </div>
                </form>
                 
               
            </div>
        </div>
    </div>
    <hr>
</body>
 
